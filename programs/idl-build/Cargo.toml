[package]
name = "markov-registry-idl-build"
version = "0.1.0"
edition = "2021"
description = "Generates the Anchor IDL of programs/strategy-registry without the Anchor CLI"
license = "Apache-2.0"
publish = false

[workspace]

[dependencies]
anchor-lang-idl = { version = "=0.1.2", features = ["build"] }
anyhow = "1"
serde_json = "1"
