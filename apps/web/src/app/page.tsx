export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-2xl flex-col justify-center gap-4 px-4 py-12">
      <p className="text-caption font-semibold tracking-[0.2em] text-text-muted uppercase">
        markov
      </p>
      <h1 className="text-heading-lg font-semibold">Build a stock strategy you can explain.</h1>
      <p className="text-body text-text-muted">
        Give it only the permissions you choose. The Mark I application shell and its navigation
        arrive in session F02; this route is intentionally minimal until then.
      </p>
    </main>
  );
}
