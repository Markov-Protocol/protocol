# Provider and capability readiness

The database table `capability_readiness` is the runtime record
(`markov db status`, `GET /v1/platform`). This file is the reviewed narrative
and must agree with it. States: `IMPLEMENTED`, `FIXTURE_VERIFIED`,
`LIVE_READ_VERIFIED`, `LIVE_WRITE_VERIFIED`, `BLOCKED`, `DISABLED`.

| Capability                      | State after B01   | Evidence / blocker                                                                                   | Activation conditions |
| ------------------------------- | ----------------- | ---------------------------------------------------------------------------------------------------- | --------------------- |
| platform.api.health             | IMPLEMENTED       | `apps/api/test/app.test.ts`, `boot.test.ts`, `scripts/ci/startup-check.sh`                            | n/a |
| platform.db.migrations          | IMPLEMENTED       | `packages/db/test/db.test.ts` against PostgreSQL 16.13                                               | Managed PostgreSQL with PITR before staging |
| platform.worker.temporal        | IMPLEMENTED       | `apps/worker/test/worker.test.ts` against Temporal CLI 1.9.1 dev server                              | Namespace, TLS/API key and task-queue review before staging |
| solana.rpc.read                 | FIXTURE_VERIFIED  | `packages/solana-rpc/test`; live endpoints BLOCKED by egress in B01 (SR-SOL-01)                       | `markov solana probe` succeeds against primary and independent secondary; genesis matches |
| solana.rpc.submit               | FIXTURE_VERIFIED  | `sendTransaction` (base64, preflight on), `simulateTransaction`, `getLatestBlockhash`, `getBlockHeight`, `getSignatureStatuses`, `getTransaction` (with token balances and loaded addresses) and `getProgramAccounts` implemented and verified against the fixture chain for registry publication (B08) and the execution lifecycle (B10); no transaction has been sent to a live cluster | A live submission recorded on devnet |
| catalog.prestocks.ingest        | BLOCKED           | Pipeline IMPLEMENTED and FIXTURE_VERIFIED with synthetic feeds (B03): sanitised snapshots, quarantine, counterfeit and collision rules, on-chain mint verification, operator admission, public search. The live PreStocks endpoint and schema are unreachable (OD-17); no live read exists. | Feed endpoint, schema mapping and terms verified from an environment with access; first live snapshot recorded |
| catalog.xstocks.ingest          | BLOCKED           | Pipeline IMPLEMENTED and FIXTURE_VERIFIED with synthetic products and events (B04): Token-2022 extension policy, scaled-amount multiplier evidence, exact raw/scaled quantities, corporate-action lifecycle. The live xStocks assets, corporate-action and multiplier endpoints are unreachable (OD-18); no real xStocks mint was read. | Endpoints, schema mapping, one real admitted mint and its multiplier verified from an environment with access |
| catalog.tessera.ingest          | DISABLED          | Not started (B17); endpoint returned 403 during planning                                             | Current API access, terms, mint identities verified |
| identity.provider.verify        | IMPLEMENTED       | Provider-neutral JWT/JWKS verification (`@markov/auth`) exercised end to end with the in-process test issuer (B02). Privy issuer/audience/key facts BLOCKED (docs.privy.io denied) | Production issuer, audience and JWKS URL verified against provider documentation; test issuer refused outside local/test (enforced) |
| policy.eligibility.rules        | BLOCKED           | Eligibility, terms, limits, capability states, policy evaluation and reservations IMPLEMENTED and FIXTURE_VERIFIED (B05) with user-assigned-code fixtures; no counsel-approved rule set or terms text exists (OD-06) and no residency/KYC provider is integrated. | Counsel-published rule set and terms; beta caps approved (OD-12) |
| execution.jupiter.quote         | FIXTURE_VERIFIED  | Execution planning (B09) IMPLEMENTED: integer allocation, fee budget, per-constituent quote checks against the reviewed program matrix, per-constituent policy decisions, hashed plans with bounds and validity, owner acknowledgement by hash. Quotes come through the Markov quote contract from the fixture venue (local/test only, plans labelled `fixture`) or an operator-configured gateway URL; both are fixture-verified. The live Jupiter quote API is unverified: every Jupiter host is unreachable from the build environment (OD-21, SR-JUP-01); no live quote has been taken. | Gateway or direct mapping written from the current Jupiter documentation, a live quote for an admitted mint recorded, route programs reviewed into the matrix |
| execution.jupiter.build         | FIXTURE_VERIFIED  | Transaction building (B10) IMPLEMENTED: the fixture venue builds legacy swaps through the fixture route program and a configured gateway may build through `EXECUTION_VENUE_BUILD_URL` (Markov build contract). Every built byte is decoded (legacy and v0 with lookup tables), validated against the acknowledged plan (fee payer and sole signer, owner token accounts, mints, exact input and minimum output, compute budget within the fee cap, account creation for the owner only, reviewed route programs, no transfers, approvals, authority changes or closures) and simulated before it is stored; eight malicious outputs are refused in `apps/api/test/execution.test.ts`. Whole-basket composition (B11) IMPLEMENTED: the fixture venue composes every leg into one transaction (account creations first, swaps in leg order from the owner's one stablecoin account), measured against the packet limit and simulated on the node at plan time; a basket is atomic only with that evidence and staged otherwise, with the reason in the plan. The multi-leg validator requires exactly one swap per leg within its bounds. No live route builds or composes: the Jupiter swap interface is unverified (OD-21), the matrix reviews no live program and the configured-URL venue has no compose contract yet. | A live build from the current Jupiter documentation decoded and validated; its route programs reviewed into the matrix |
| execution.spot.submit           | FIXTURE_VERIFIED  | Submission and recovery (B10) IMPLEMENTED against the fixture chain: owner signature verified over the exact prepared message, policy re-evaluated at stage `submit` with a reservation, attempt persisted with its signed bytes before the one broadcast, chain-derived states (submitted, confirmed, finalized with fills from transaction meta, failed on a landed error or a preflight verdict, expired unseen, unknown until reconciled), the same bytes resent while the blockhash lives, at most one live attempt per intent, read-time and durable (Temporal) reconciliation, cancel before signature and cancel-requested after broadcast. Buys and sells verified end to end (`docs/markov/execution-state-machine.md`). Baskets (B11) verified against the fixture chain: an atomic two-constituent transaction with one signature, one reservation per leg and a fill per leg; a staged basket landing leg by leg with the intent returning to the owner between transactions, a later leg quoted again and refused when its fresh terms miss the approved bounds, partial completion on a later failure, expiry or cancel, and a reviewed completion of exactly the unfilled legs at their original targets (`apps/api/test/basket-execution.test.ts`). Nothing has been sent to a live cluster; `EXECUTION_WRITES_ENABLED` stays false outside the fixture runs. | A devnet buy and sell recorded with signatures and fills; live-route limitations reviewed |
| accounting.journal              | FIXTURE_VERIFIED  | Append-only quantity journal (B12): settled fills, network fees and rent projected once per signature and leg into entries balanced per asset in raw units, FIFO lots attributed to the one matching strategy instance or kept at wallet level, sells consuming lots oldest first, chain reconciliation with checkpoints that records every unexplained difference as an external flow the owner acknowledges, holdings per wallet and per instance without valuation. Verified against the fixture chain (`apps/api/test/accounting.test.ts`, `scripts/ci/startup-check.sh`): duplicate projections change no total, a trade reconciles without external flows, a transfer made outside the platform is detected and the strategy's lots stay untouched. No live balance has been reconciled. | A devnet wallet reconciled against a live node, with a real deposit and withdrawal acknowledged |
| receipts.signing                | FIXTURE_VERIFIED  | Canonical decision and execution receipts (B12) signed with a versioned Ed25519 key, published verification keys, offline verification by the CLI and the SDK helper, public redaction on opt-in. The key comes from configuration (`RECEIPT_SIGNING_PROVIDER=local_key`, refused in production); the KMS-backed signer is not implemented (OD-22). A signature attests to the record; settlement is the chain evidence the record references. | KMS-backed signer with key rotation and a retired key verified against a receipt it signed |
| analytics.performance           | FIXTURE_VERIFIED  | Valuation and performance (B13): recorded reference observations (issuer feeds on ingestion, operator entries with evidence; never quotes) resolved by kind precedence and freshness, historical multipliers, model and actual series on a daily grid with a point at every external flow, chained time-weighted and Modified Dietz returns, drawdown, turnover, realized and unrealized P&L, fees, completeness and model-only rankings with the 30-day product rule. Verified against fixture vectors derived independently in Python (`packages/analytics/fixtures`) and against the fixture chain (`apps/api/test/analytics.test.ts`, `scripts/ci/startup-check.sh`): a deposit is not a return, a split leaves the value alone, a missing or stale observation reports no return and no rank. No live price source is integrated (OD-23); a production series is incomplete rather than invented. | A live reference price feed and a SOL price source recorded with terms and freshness (OD-23); one public version ranked over 30 days of live observations |
| registry.strategy.publish       | FIXTURE_VERIFIED  | Anchor program `programs/strategy-registry` IMPLEMENTED and verified under `solana-program-test` (real runtime, native processor): registration evidence, duplicate initialisation, unauthorized publishing, seed and account misuse, weight overflow and every rule, lineage, immutable content; shared vectors reproduced by `@markov/registry`; publication flow (prepare, what-becomes-public preview, byte-exact signed submission, chain-derived states, indexer, public verification) verified against the fixture ledger and PostgreSQL (B08). No SBF artifact was built and no validator ran: platform tools and Agave releases are unreachable from the build environment. Publication is disabled unless `REGISTRY_PROGRAM_ID` is configured; mainnet needs production mode and release evidence. | Validator run with a deployed devnet program recorded; reproducible build hash; independent review (OD-09); upgrade-authority multisig and change process (OD-10) |
| research.model.generate         | BLOCKED           | Manual research (theses, revisions, safe source retrieval, deterministic mapping) IMPLEMENTED and tested (B06); the bounded adapter contract, output validation and provenance are FIXTURE_VERIFIED with the deterministic fixture adapter, which configuration refuses outside local/test. The xAI adapter (`@markov/model-xai`, B17) is IMPLEMENTED against the OpenAI-compatible chat completions contract and verified against an in-process stand-in (bearer, timeout, byte cap, redacted errors, strict JSON, only the run's own sources and candidates survive); no live call was made because api.x.ai is unreachable from the build environment (SR-XAI-01), and no live page has been retrieved (the https transport is tested through its in-memory stand-in only). | `XAI_API_KEY` configured; one live research run recorded here with the model name; provider terms, retention and data handling recorded in `docs/markov/agent-permissions.md`; retrieval exercised against a live public page; no signing access |
| companion.model.run             | BLOCKED           | Typed agent tools over the domain services with the caller's own authority are IMPLEMENTED and tested (B15); the bounded companion loop (budgets, strict tool inputs, refusals recorded, redacted provenance, owner-only proposals, Mark I event log) is FIXTURE_VERIFIED with the deterministic fixture adapter, which follows injected directives so the refusals are exercised, and which configuration refuses outside local/test. The xAI adapter (B17) is IMPLEMENTED: one JSON step per call, prose treated as an answer and never as a call, usage priced in micros from configured per-token rates; verified against an in-process stand-in only (SR-XAI-01). | `XAI_API_KEY` configured; an adversarial run against the live provider with the fixture document refused at the tool layer, recorded here; provider terms, retention, model pin and price list recorded in `docs/markov/agent-permissions.md` |
| maintenance.scheduler           | IMPLEMENTED       | Schedules, occurrences with dedup keys, missed-run policies, review expiry, pause/cancel/revoke, drift-driven rebalance proposals that open into reviewed intents, the in-app notification outbox and the worker loop (B16), tested against PostgreSQL, the Temporal dev server and the fixture chain (`apps/api/test/maintenance.test.ts`, `apps/worker/test/worker.test.ts`, `scripts/ci/startup-check.sh`); every occurrence prepares a proposal for the owner's session and nothing spends | A worker credential and `MAINTENANCE_API_URL`/`MAINTENANCE_API_TOKEN` set on the deployed worker; the loop observed ticking in staging |
| notifications.email             | BLOCKED           | The durable in-app outbox, category preferences, address verification by hashed code, per-delivery retries with backoff, dead letters and operator requeue are IMPLEMENTED and tested (B16); the email adapter contract and rendering (no credential, no bearer link, stable idempotency key) are FIXTURE_VERIFIED with the recording fixture, which configuration refuses outside local/test; the configured HTTP adapter has been exercised against an in-memory stand-in only. No provider is integrated (OD-25). | Provider, sender domain (SPF/DKIM/DMARC), terms and retention recorded; a live verification message received; webhook or bounce handling reviewed |
| liquidity.meteora.read / dbc-simulate | DISABLED    | Not started (B17)                                                                                    | Read-only pool observation; launches stay disabled |
| automation.unattended           | DISABLED          | Deliberate release gate                                                                              | Independently enforced scoped authority, revocation evidence, separate review and pilot |

Changing a row requires evidence in this file and, for production, an
attributable operator action (`upsertCapabilityReadiness` records `updated_by`).
