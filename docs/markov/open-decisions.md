# Open decisions

Decisions that need real evidence, credentials or an accountable owner.
None blocks local implementation of independent modules. Each entry names
the session where it must be resolved at the latest.

| ID    | Decision                                                                 | Needed by | Owner (to assign) | Notes |
| ----- | ------------------------------------------------------------------------ | --------- | ----------------- | ----- |
| OD-01 | Solana SDK family (`@solana/kit` vs `@solana/web3.js`) and conversion isolation | resolved (B10) | engineering | Resolved by ADR-0009: no SDK. `@markov/solana-codec` implements the wire formats (legacy and v0 messages, lookup tables, token layouts, Ed25519, the fixture chain) and `@markov/execution` decodes and validates every instruction itself; `@solana/*` stays unowned in the boundary rules. Reopen only if a live route needs a provider decoder, which then enters one integration package behind fixture tests. |
| OD-02 | TypeScript 6/7 upgrade                                                   | any       | engineering | Re-run the full gate; note in ADR-0002. |
| OD-03 | OpenTelemetry exporter and tracing backend                               | B18       | operations | Logs are structured now; traces from request to receipt are required before release candidate. |
| OD-04 | Primary and independent secondary RPC providers, credentials and rate limits | B03 | operations | Required for staging; keys must be redacted from URLs in logs (implemented). |
| OD-05 | Production identity-provider account (existing provider or Privy)        | F03       | product/security | B02 shipped a provider-neutral verifier (issuer, audience, JWKS, asymmetric algorithms). Privy documentation was unreachable; confirm its issuer string, audience (app id), key publication (JWKS vs PEM) and `auth_time` semantics, then add PEM support if needed. |
| OD-06 | Launch jurisdictions, product classification, distribution permissions   | before beta | counsel/product | Backend records versioned decisions under operator-published rule sets and terms (B05); until counsel publishes real rules every declaration is `unknown` and blocks execution. Only user-assigned ISO codes exist as fixtures. |
| OD-07 | First admitted instruments (PreStocks/xStocks) and issuer terms          | B03/B04   | product/counsel | Admission needs on-chain mint verification and terms review. |
| OD-08 | Licensed research/price data rights                                      | B06/B13   | product | A reachable endpoint is not a redistribution right. B06 stores only a bounded plain-text excerpt (≤ 1,200 characters), the content hash and the URL of a source; whether longer quotation or redistribution of a given publisher's text is permitted stays undecided. |
| OD-09 | Independent reviewers and review scope (backend, registry program)        | B08/B18   | security | Program (`programs/strategy-registry`, Anchor 0.31.1, two instructions, one account type, program-test suite) and the backend verification path (`@markov/registry`, `apps/api/src/registry`) are ready for review since B08; no reviewer named yet. No unresolved critical/high findings before beta. |
| OD-10 | Registry upgrade authority signers and change process                    | B08       | security/ops | B08 deploys nothing: the program id in `declare_id!` is a placeholder whose secret was discarded, and the deployment keypair and upgrade authority must be minted and held by an independent multisig with a written change process before the first devnet deployment (`docs/markov/strategy-registry.md`, "Deployment and review"). Never renounced automatically. |
| OD-11 | Hosting provider, budget and operations ownership                        | B18       | operations | One write region with PITR; compose file is a local convenience only. |
| OD-12 | Approved mainnet beta caps (`BETA_*`) and participant allowlist          | before beta | product/risk | Config refuses production writes without them; B05 applies the caps as the limit ceiling and enforces the allowlist (`beta_participants`) when enabled. The approved values are still undecided. |
| OD-13 | Hardware capability and pairing model for Mark I                         | B15       | product/hardware | Event contracts only in this repository. |
| OD-14 | Future perps venue                                                       | after stock release | product | No venue selected by the specification. |
| OD-15 | Tessera API access, schema and terms                                     | B17       | product | Endpoint returned 403 in planning; disabled until verified. |
| OD-17 | PreStocks product feed endpoint, response schema, terms and mapping to the Markov issuer feed contract | B03 follow-up, before any live ingestion | product/engineering | prestocks.com and its documentation were unreachable from the build environment. The pipeline consumes the Markov feed contract from a fixture or an operator-configured https URL; the PreStocks-specific mapping must be written and live-read-verified from an environment with access, and admission additionally needs the terms review recorded as evidence. |
| OD-18 | xStocks assets, corporate-action and multiplier endpoints, schema mapping and at least one real admitted mint | B04 follow-up, before any live ingestion | product/engineering | xstocks.com and its documentation were unreachable. The listed-stock pipeline consumes the Markov product and corporate-action feed contracts from fixtures or operator-configured https URLs; the real mapping and a live mint read (extension set, scaled multiplier) must be verified from an environment with access before `configured_url` ingestion is enabled. |
| OD-19 | Research model provider, model version, data-handling terms and egress for source retrieval | before any run outside local/test | product/security | B06 ships the adapter contract with a deterministic fixture only (`RESEARCH_MODEL_PROVIDER` refuses it outside local/test). A hosted provider needs its terms, retention and the exact data sent recorded in `docs/markov/agent-permissions.md`; the deployment's egress must allow public https for citations while the policy keeps refusing internal names. |
| OD-20 | Strategy publication, registry program and moderation: publisher-wallet binding, upgrade authority, deprecation semantics, what a public listing shows and who can hide one | B08, before any publication | product/security | Decided in B08 (ADR-0008): the publisher wallet (one of the owner's verified wallets on the deployment's network) signs, pays and is the only status authority; records are keyed by manifest hash and never rewritten; deprecation is a reversible status byte set by the publisher only; the public API lists registered versions whose `moderation` is `none`, and moderation hides a listing without touching the chain record. Open: who may set `moderation` (no route exists yet; it is a database flag) and the upgrade-authority holders (OD-10). |
| OD-21 | Execution venue: the current Jupiter quote and swap interface, its terms, credentials, supported mints and route programs | before any live quote or build (B10 delivered the lifecycle against fixtures only) | engineering/product | Every Jupiter host (dev.jup.ag, station.jup.ag, docs.jup.ag, api.jup.ag, lite-api.jup.ag, quote-api.jup.ag) was unreachable from the build environment in B09 (SR-JUP-01). Plans consume the Markov quote contract from the fixture venue or an operator-configured gateway; no live quote exists and the route/program matrix lists no live program. Before a live plan: read the current documentation, map the real response onto the contract (gateway or adapter), record a live quote for an admitted mint, review the route programs into the matrix with evidence, and decide the API-key handling. B10 adds the build contract (`EXECUTION_VENUE_BUILD_URL`, `venueBuildRequestSchema`/`venueBuildResponseSchema`): a gateway's bytes are decoded, validated against the plan and simulated before anyone signs, but every live route program must be reviewed into the matrix or the validator refuses it. B11 adds the compose port (every leg in one transaction) implemented by the fixture venue only; a live basket stays staged (`composition_unavailable`) until a gateway compose contract is specified and verified. |
| OD-23 | Reference price sources for valuation and rankings: which issuer marks, secondary-market prices and underlying equity prices may value each instrument, a SOL price source for fees and wallet series, stablecoin observations for depeg detection, their terms, freshness and evidence; whether daily valuation snapshots are persisted for rankings | before any public ranking or performance figure outside local/test (B13 computes series from the fixture feeds' prices and operator entries only) | product/engineering | B13 defines the observation record (`price_observations`: kind, unit, time, source, source kind, evidence), the precedence (secondary market, issuer mark, underlying equity as a stated assumption; implied valuation never) and the 24-hour freshness rule, and computes every series on read. Decide the live sources per issuer, whether the token's own market or the underlying's price is authoritative, how SOL and the stablecoin are observed, and the snapshot job once the population of public versions makes on-read ranking too slow. |
| OD-22 | Receipt signing key management: KMS-backed Ed25519 signer, key identifiers, rotation and retirement, publication of the verification keys | before any receipt is issued outside local/test (B12 issues receipts with a configured local key that production refuses) | operations/security | B12 defines the receipt format, the canonical bytes (`markov-receipt/v1`), the `ReceiptSigner` port, the key registry (`receipt_signing_keys`, active and retired keys) and offline verification. `RECEIPT_SIGNING_PROVIDER=kms` is refused until a signer exists behind the port; choose the KMS (and whether it signs the canonical bytes directly or a hash), the rotation cadence, how retired keys keep verifying older receipts, and who may rotate. |
| OD-16 | Docker-based local dependency path verification                          | next environment with Docker | engineering | `docker-compose.yml` was not executed in B01. |
