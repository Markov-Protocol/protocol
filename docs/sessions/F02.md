# Session F02 — Full-screen Mark I shell and home

Session: F02 — full-screen Mark I workspace
Date: 2026-09-24 · Branch: `claude/affectionate-gauss-2ml7ll`

## Behavior delivered

- `packages/markov-shell` (`@markov/shell`): `DeviceFrame` (cream perimeter,
  black screen, decorative sensor and wordmark), `PixelEyes` (SVG 5×7
  matrices, blink under motion preference), `CompanionPresence` (eyes plus
  textual status), `TopBar`, `Navigation` (rail and bottom variants with
  `aria-current`), `SkipLink`, `ShellModeProvider`/`useShellMode`
  (companion/workspace/review modes, per-viewer focus preference with
  storage failure tolerance) and the composed `MarkovShell`; `shell.css`
  with `100dvh` and a `100vh` fallback, safe-area padding, content-driven
  breakpoints (640 and 1024 CSS px) and reduced-motion behaviour.
- `apps/web`: `AppShell` composition with Next.js links, route inventory
  with titles and delivering sessions, a More menu (Activity, Rankings,
  Automations, Settings, focus toggle), the companion home (anonymous
  variant live; auth-loading variant ready for F03), honest unavailable
  pages for every navigation target, the component reference rendered
  inside the shell, `viewport-fit=cover` and theme colour.
- Evidence: Playwright at 320, 390, 768, 1280 and 1440 CSS px with
  screenshots, scroll containment, deep links, More menu, focus persistence,
  skip link and a lab performance baseline.

## Backend and provider contracts used

None. The top bar reports "No backend connected" and "Not signed in"
truthfully until F03.

## Readiness

IMPLEMENTED and FIXTURE_VERIFIED. No live evidence applies.

## Verification

`docs/frontend/verification.md` (F02 section): 43 jsdom tests, 28
Playwright checks, production build with 11 routes, `pnpm verify` green.

## Visual evidence

`docs/frontend/evidence/F02/home-*.png`, `explore-*.png`, `focus-1280.png`,
`perf-baseline.json`.

## Documentation updated

`docs/frontend/mark-i-shell.md` (new), `routes-and-journeys.md`,
`verification.md`, `README.md`; root `README.md`.

## Commit

`feat(shell): add the full-screen Mark I workspace` (hash in branch history).

Bound in session P01 from the branch history (full hashes in [`docs/markov/release-status.json`](../markov/release-status.json)):

- `16407890a574` feat(shell): add the full-screen Mark I workspace (pre-rewrite `61e4bdd`)

## Open dependencies and release impact

- Sign-in, wallet and connection state are placeholders until F03/F04.
- Font transfer (about 740 KB for two Inter variable files) dominates the
  home payload; subsetting or dropping the italic file is an open item
  before any performance budget is set.
- Mobile keyboard/`VisualViewport` behaviour and screen-reader journeys
  remain unverified on devices (tracked in `accessibility.md`).
- The review mode and companion panel are empty until F09 and F14.

## Next session and first concrete action

Backend B02 (verified accounts and wallet ownership) is the prerequisite
for F03. First action: start B02 as specified in the backend master prompt
(`packages/auth`, identity records migration, challenge-based wallet
linking, scoped API credentials, owner-scoped query helpers, operator
separation, device principal contracts); then F03 integrates the session
contract into the shell.
