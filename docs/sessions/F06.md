# Session F06 — Instrument research and saved theses

Date: 2026-09-24 · Branch: `claude/affectionate-gauss-2ml7ll` · Commit:
`feat(research): connect instrument evidence and strategy theses` (hash in
branch history).

## Delivered

- Backend (additive, in the backend's own architecture): `GET
  /v1/me/theses` carries `instrumentIds` of the current revision and takes
  `?instrumentId=` (jsonb containment in the research store); API test,
  OpenAPI document and `@markov/api-client` regenerated.
- App proxy: research operations (theses, revisions, sources, mappings,
  runs, cancel, the public projection) and the B07 draft operations
  (`/v1/strategies/limits`, `GET`/`POST /v1/me/strategies`) allowlisted;
  `PATCH` forwarded under the same same-origin and JSON rules as `POST`
  and exported by the App Router handler; `useMarkovApi.patch`; 18 new
  contract-matrix rows; "Research" in the More menu; `/research` in the
  route registry.
- `/research`: workspace listing the person's theses (visibility, status,
  revision, instrument count) and a form that starts a private thesis.
- `/research/[thesisId]`: the owner's editor: title and claim; statements
  typed as sourced fact, issuer assertion, opinion or (from runs only)
  model interpretation, with topics and citations ticked from fetched
  sources; counterarguments; shortlist by canonical id with a catalog
  picker and notes; research subjects with deterministic mapping to
  admitted instruments; sources panel (attach by https URL, cards with
  fetched/refused/failed status, role, dates, plain-text excerpt, hash,
  https-only links); bounded runs (question, ticked sources, polled
  progress, cancel, labelled output, suggested instruments confirmed one
  by one, unmatched companies as subjects, dropped identifiers,
  provenance; "unavailable" without a provider); private notes marked as
  never public; Save appending an immutable revision with Saving / Saved
  as revision N / failure-with-edits-kept states, local checks by field
  and API rules mapped to fields, a two-tab notice for a newer revision;
  Publish with "what becomes public"; make private; archive/restore;
  saved revisions loadable into the editor; the public view preview.
  Anyone else sees the published projection or "not found or private".
- Shortlist to basket: equal integer weights with the exact cash remainder
  stated first, then a B07 draft linked to the thesis; the backend's
  validation shown verbatim; "Open in Build".
- `/strategies/new`: lists the person's basket drafts (highlighting the
  one just created) until the F07 editor arrives.
- Market detail: Research tab (evidence rules, theses mentioning the
  instrument, start a thesis with it shortlisted, sign-in prompt for
  anonymous people), "Rights and evidence" on the Overview, "Add to a new
  basket draft" (one constituent at 100.00%), Liquidity as route
  observations with every field "Not observed".
- Docs: frontend README, routes and journeys, contract map, verification,
  security and privacy, backend `api.md`, README.
- Test infrastructure: `withTemporaryDatabase` retries a forced drop that
  PostgreSQL refuses transiently (42501/55006) because a session this role
  may not signal is attached at that instant; the identity test's
  secret-leak assertion now compares the whole base64url secret instead
  of a fragment cut at an underscore (both surfaced as flakes in the first
  verify run of this session; no product code changed).

## Commands and results

| Command | Result |
| ------- | ------ |
| `pnpm exec vitest run --project node apps/api/test/research.test.ts` | 3 passed (list filter and `instrumentIds` asserted) |
| `pnpm exec vitest run --project node apps/web/test/server/proxy.test.ts` | 12 passed |
| `pnpm exec vitest run --project web` | 15 files, 89 tests passed (research 11, editor helpers 5, markets 12 updated for the new Research and Liquidity tabs) |
| `pnpm exec playwright test e2e/research.spec.ts` (real API, fixture source and adapter) | 4 passed: the research journey and the private-access journey on desktop and phone |
| `MARKOV_TEST_DATABASE_URL=… pnpm web:e2e` | 58 passed on desktop and phone profiles: 2 research journeys (below) plus the F01 to F05 suites; each profile uses its own accounts |
| `pnpm verify` (PostgreSQL and the Temporal dev server running) | green: Biome clean (pre-existing warnings only); typecheck clean; 54 files / 325 tests passed; boundaries clean (22 workspace packages); `docs/markov/openapi.json` current; api-client types current; migrations match the schema; tokens current; web build 22 dynamic routes |

## Provider verification status

No provider is involved beyond B06's fixture model adapter, which the
backend refuses outside local/test; runs in this session are
FIXTURE_VERIFIED. Retrieval used the in-memory fixture source; no live
page was fetched. Everything else is IMPLEMENTED and tested against the
local API. No funds, mandates, orders, publications on chain or messages
were involved; a published thesis is a database projection, not an
on-chain registration.

## Unresolved risks and dependencies

- A hosted model provider, its terms and its data handling remain OD-19;
  the run UI shows "unavailable" until one exists.
- Citations open source records; the outbound link is offered only for
  https destinations and never fetched by the browser. Publication rights
  for longer quotation remain OD-08.
- Thesis saves are explicit (no autosave) because every save is an
  immutable revision; the two-tab case is detected and announced rather
  than merged.
- The basket draft created from a shortlist uses equal weights only as a
  stated starting point; the editor that changes weights, applies the
  policy limits and activates is F07.
- Screen-reader journeys through the editor are not yet recorded.
- A superuser-owned PostgreSQL session exists in this environment
  (`pg_stat_activity` shows a `postgres` backend whose details this role
  cannot read); the forced drop of a temporary test database is refused
  while it is attached. The retry covers the tests; CI should run the
  suite against a database with no foreign sessions.

## Next session

F07 (complete stock basket builder on the B07 routes: Research, Assemble,
Set Rules, Activate with a single draft identity, exact basis-point
editing, backend validation, revision conflict handling), then B08
(strategy registry program with the manifest hash vectors) in dependency
order.

## Commit

Bound in session P01 from the branch history (full hashes in [`docs/markov/release-status.json`](../markov/release-status.json)):

- `2ed2a5d606a2` feat(research): connect instrument evidence and strategy theses (pre-rewrite `a46900c`)
