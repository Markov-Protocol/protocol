# Session D01 — The markov.pet documentation site

Date: 2026-09-25 · Branch: `claude/affectionate-gauss-2ml7ll` · Commit:
`docs(site): add the markov.pet documentation site` (`b0a23e9` after the
2026-09-25 author rewrite, pre-rewrite `88b193d`; full hash in
`docs/markov/release-status.json`).

## Delivered

- `apps/docs` (`@markov/docs`): a Docusaurus 3.10.2 site with site `url`
  `https://markov.pet` and `baseUrl` `/docs/`, so every route is exactly
  the path it has under markov.pet/docs. Broken links, broken anchors,
  broken Markdown links and broken images fail the build (`onBrokenLinks: 'throw'`
  and the Markdown hooks). Mermaid diagrams render; local search
  (`@easyops-cn/docusaurus-search-local`, no external service) indexes
  every page at build time.
- Nothing is written twice. `scripts/sync-content.mjs` copies every
  document under `docs/markov`, `docs/markov/adr`, `docs/frontend`,
  `docs/frontend/design-reference` and `docs/sessions` into
  `apps/docs/docs/reference/…` at build time with front matter (title,
  sidebar label and position, description, `custom_edit_url` to the source
  file), a "Source" admonition, relative links between synced pages, and
  links to evidence, code or fixtures rewritten to the repository (blob or
  raw URLs); `<placeholder>` text outside code is escaped for MDX.
  `scripts/generate-api-reference.mjs` renders `docs/markov/openapi.json`
  into one page per tag (135 operations, 13 tags) with parameters, request
  body and response schema tables. `scripts/generate-cli-reference.mjs`
  loads the built CLI program (`apps/cli/dist/index.js`) and walks the
  commander tree (20 command groups) so every flag comes from the code.
  Generated pages are git-ignored; the edit link of a generated page
  points at its source document, the OpenAPI document or `program.ts`.
- Hand-written pages whose commands are the ones
  `scripts/ci/startup-check.sh` runs: introduction, run locally, the
  startup check (its fifteen journeys in order), the app, the CLI flows
  (person, operator, policy, research and strategies, plan-sign-execute,
  holdings and receipts, performance and prices, database and operations,
  every command and flag checked against `apps/cli/src/program.ts`), API
  conventions (principals, the error envelope, idempotency, rate limits,
  what an answer does and does not mean), accounting and performance,
  security, contributing (a restatement of `AGENTS.md`).
- The Markov theme: `packages/ui/src/styles/tokens.css` is read at build
  time into `src/css/markov-tokens.generated.css`; Inter is served from
  `apps/web/src/assets/fonts`; the pixel eyes mark; the Mark I frame with
  a cream perimeter (`#E6DCD1`), a dark screen by default (`#070909`,
  periwinkle accent 7.53:1) and a light screen on the toggle (`#F3ECE4`,
  the design system's darker accent `#3F55C9` at 5.35:1 on the frame);
  the navbar and footer stay cream in both modes; a social card rendered
  from the same frame; the homepage with the four steps and the six entry
  cards; a "How to read these pages" section that states what a
  fixture-verified capability is and is not.
- Integration for markov.pet/docs: the app (`apps/web`) reads
  `MARKOV_DOCS_ORIGIN` (a bare origin, https outside local and test,
  never a path) and rewrites `/docs` and `/docs/:path*` to it; unset means
  `/docs` is not served, with no fallback origin (`web-env.ts`,
  `next.config.ts`, six web environment tests). `.env.example` documents
  it.
- Workspace: `@markov/docs` is classified as an app in
  `tooling/boundaries/rules.json` (it may import React and nothing from
  the backend or the frontend packages); Biome ignores the generated
  output; root scripts `docs:start`, `docs:build`, `docs:serve`,
  `docs:e2e`, `docs:typecheck`; `pnpm typecheck` includes the site and
  `pnpm verify` ends with `pnpm docs:build`; the CI workflow gained steps
  that build the site and run its browser checks after the web evidence
  (GitHub Actions has not run on this repository; runs start with P01).
- Verification of the site itself: `apps/docs/e2e/docs.spec.ts` runs
  Playwright against the served production build at 1280 and 320 px
  (home with the frame in both modes, the four steps and the entry points;
  the sidebar or the phone drawer to the API reference; the analytics
  routes page with `GET /v1/rankings/model`; the CLI performance page; the
  accounting methodology with its edit link to the source file; the B13
  session page; search results for a word), with screenshots under
  `docs/frontend/evidence/D01/`.
- Docs: `README.md` (verification commands, layout, docs commands),
  `docs/markov/operations.md` ("Documentation site"),
  `docs/frontend/README.md` (`MARKOV_DOCS_ORIGIN`, `pnpm docs:start`),
  `docs/markov/open-decisions.md` (OD-24, hosting of the site).

## Design notes

- The site is a build product of the repository, so a document, a route
  or a command cannot drift from the site without a build failing or the
  page changing; "the documentation works as the reference for the actual
  working" holds because the reference pages are generated from the
  contract the API exports and the program the CLI runs, and the guides
  quote the startup check that the CI workflow invokes.
- Generated pages are not committed: the diff of a change stays in the
  source document, the route or the command, and the site follows.
- `readme.md` files under a synced folder become the folder's index page
  (`/docs/reference/frontend`, `/docs/reference/frontend/design-reference`),
  which is Docusaurus's category-index convention.
- The docs app has no `"type": "module"`: webpack treats `.js` under an
  ESM package as strict ESM and Docusaurus's server bundle then fails with
  `require.resolveWeak is not a function`; the scripts are `.mjs` files
  and unaffected.
- Dark is the default regardless of the OS preference
  (`respectPrefersColorScheme: false`): the Mark I screen is the product's
  identity, and the toggle remains for anyone who prefers the light
  screen. The dark variables are declared on `html[data-theme="dark"]`
  because the theme's own declaration has that specificity.
- At D01 the site claimed no deployment: OD-24 records that hosting the
  built output under `markov.pet/docs` is a deployment decision outside
  this repository; D01 deployed nothing. Update (B17, 2026-09-25): the
  product owner chose Vercel, and B17 deployed the site
  (https://markov-docs.vercel.app/docs/) and the app, which proxies
  `/docs` to it through `MARKOV_DOCS_ORIGIN`; both were built from commit
  `7840da2` (rewritten as `9b6bde3`, identical tree). markov.pet itself is
  not routed to these deployments.

## Commands and results

| Command | Result |
| ------- | ------ |
| `pnpm install` (docs app added, `pnpm-lock.yaml` updated; all versions older than the 24-hour minimum release age) | ok |
| `pnpm --filter @markov/docs run prepare-content` | 64 pages synced under `docs/reference`; 135 operations in 13 tag pages; 20 CLI command groups |
| `pnpm --filter @markov/docs exec docusaurus build` | first runs failed on: named exports in `docusaurus.config.ts` read as unknown config fields (removed), unquoted front matter with colons (quoted), `require.resolveWeak` under `"type": "module"` (removed), 111 links to `/reference/frontend/readme` (the category index is `/reference/frontend`); then `[SUCCESS] Generated static files in "build"` with broken links and anchors as errors |
| `pnpm --filter @markov/docs typecheck` (after `docusaurus clear`, so without the generated cache) | clean (needed `@types/react` and `clsx()` around optional CSS-module class names under `exactOptionalPropertyTypes`) |
| `pnpm exec vitest run --project web apps/web/test/web-env.test.ts` | 6 tests passed: `MARKOV_DOCS_ORIGIN` accepted as a bare origin, refused with a trailing slash, a path or a non-URL, refused as http outside local and test, null when unset |
| `pnpm --filter @markov/docs e2e` (served production build, `docusaurus serve` on 127.0.0.1:3200, desktop 1280×800 and phone 320×640) | 6 passed (10.2 s): home with the eyes, the four steps and "Start here" to `/docs/intro`, `html[data-theme=dark]`, navbar `rgb(230, 220, 209)`, screen `rgb(7, 9, 9)`, then the toggle (in the phone drawer at 320 px) to `light` with the screen `rgb(243, 236, 228)` and the navbar still cream; the sidebar (desktop) or the drawer's main menu (phone) to `/docs/api` with `/v1/rankings/model` listed, the analytics page heading `GET /v1/rankings/model` with `minHistoryDays`, the CLI page heading `markov performance rankings`, the methodology page with "Valuation and performance (B13)" and an edit link to `docs/markov/accounting-methodology.md`, the B13 session page with an edit link to `docs/sessions/B13.md`; typing "rankings" opens the listbox with results naming rankings. Ten screenshots under `docs/frontend/evidence/D01/` |
| `pnpm lint`, `pnpm boundaries:check` | lint: no errors (the 3 pre-existing warnings; one new CSS specificity warning fixed by reordering); boundaries ok (31 workspace packages) |
| `pnpm install --frozen-lockfile --offline` | "Already up to date": the committed lockfile is what the CI workflow installs |
| `pnpm verify` (PostgreSQL and the Temporal dev server running) | green: lint (574 files, 3 pre-existing warnings), typecheck (backend, tests, web and docs), 82 test files / 583 tests, boundaries (31 packages), OpenAPI current, generated client current, migrations current, tokens current, the web production build, the docs build (`[SUCCESS] Generated static files`) |

## Verification

The first browser runs failed on three of my own assumptions rather than
on the site: the local search renders its results in a `role="listbox"`
without the class names I had guessed (the site's search worked from the
first build; the assertion now reads the listbox); a Docusaurus category
without a link is a button, not a link, and the phone drawer opens on the
docs sidebar so the navbar items are one "Back to main menu" up; and the
dark screen was Infima's default grey because the theme declares its
dark variables on `html[data-theme="dark"]`, one specificity level above
mine (the test caught it: `rgb(27, 27, 29)` instead of `rgb(7, 9, 9)`).
With those fixed, every check passed at both widths, and the screenshots
show the cream perimeter with the dark screen (`docs-home-dark-*.png`),
the light screen after the toggle (`docs-home-light-*.png`), the API
reference, the CLI reference and the methodology page.

The full gate ran green with the site included: `pnpm verify` ends with
`pnpm docs:build`, which syncs 64 repository documents, generates 135 API
operations and 20 CLI command groups and builds with broken links and
anchors as errors; `pnpm typecheck` includes the docs app; the CI
workflow gained the steps that build the site and run the browser checks
after the web evidence.
The browser checks ran against the build that `pnpm verify` produced. The
lockfile installs frozen. The docs site itself was not deployed; the
evidence is the served local build.

## Provider verification status

No provider is involved and no capability state changes: the site
documents the states recorded in `docs/markov/provider-capabilities.md`
and never rounds one up. Nothing was deployed: the built site was served
on `127.0.0.1:3200` for the browser checks only; markov.pet/docs goes
live when the built output is hosted under `/docs/`,
`MARKOV_DOCS_ORIGIN` is configured for the app and markov.pet is routed
to the app (OD-24; B17 did the first two on Vercel, see Design notes).
No funds were spent, nothing was published, no mandate was created and no
message was sent.

## Unresolved risks and dependencies

- Hosting (OD-24, partly decided 2026-09-25): Vercel serves the site
  (project `markov-docs`, https://markov-docs.vercel.app/docs/,
  `build:vercel` output staged under `out/docs/`, settings in
  `apps/docs/vercel.json`). Still open: routing on markov.pet itself, the
  publish job on `main` and the review step before a publication; nothing
  deploys on push until the Vercel GitHub App is installed, and no GitHub
  Actions run has built the site (runs start with P01).
- The search index is built from every page, including the session logs
  and the generated references (about 4.7 MB); it loads on the first
  focus of the search box. If it grows further, restrict the indexed
  paths or split the index by section.
- The generated API reference renders schemas to a depth of three and
  lists `anyOf` variants; a deeper or recursive schema shows as `object`
  with a link to the OpenAPI document. The CLI reference lists commands,
  arguments and options; command behaviour is documented in the guides
  and the backend contract, not extracted from code.
- The site reads Inter and the design tokens from the app and the UI
  package by relative path; moving those files breaks the docs build
  (which is the intended coupling).

## Next session

F11 (portfolio, accounting and receipts in the app), then B14. The docs
site needs no follow-up for either: new routes and commands appear in the
generated references on the next build, and a new session log appears
under Session evidence. When the app gains portfolio and rankings pages,
extend `apps/docs/docs/guides/app.md` (the routes table) in the same
commit.
