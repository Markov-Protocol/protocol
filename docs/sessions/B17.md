# Session B17 — Provider readiness and Meteora research tools (in progress)

Date: 2026-09-25 · Branch: `claude/affectionate-gauss-2ml7ll` · Status: in
progress. First increment committed as
`feat(agents): add the xAI model adapter` (hash in branch history); the
Meteora research tools and the readiness matrix follow in this session.

## Delivered so far

- Provider decision (product owner, 2026-09-25): the AI and agent side
  uses the xAI Grok API. `packages/model-xai` implements both model
  contracts over the OpenAI-compatible chat completions endpoint without
  a provider SDK:
  - `createXaiClient`: bearer authentication, `https` required outside
    local/test, no redirects, a timeout, a 1 MiB byte cap, classified
    failures (`unauthorized` 401/403 not retryable, `rate_limited` 429
    and `http` 5xx retryable, `oversized`, `malformed`, `unreachable`),
    the key redacted from every error, usage priced in micros per token
    from configured rates.
  - `createXaiResearchAdapter`: sends the canonical prompt text the run
    hashes for provenance verbatim, asks for one JSON object, validates
    it and keeps only the run's own source ids and candidate instrument
    ids as bounded plain text; a reply that is not the protocol fails the
    run (nothing is invented).
  - `createXaiCompanionAdapter`: one JSON step per call (a tool call or
    the answer); prose is treated as the answer and can never become a
    call; the loop still validates every tool, argument and citation.
- Configuration: `RESEARCH_MODEL_PROVIDER` and `COMPANION_MODEL_PROVIDER`
  accept `xai`; `XAI_API_KEY` (required by, and refused without, a
  provider set to `xai`), `XAI_BASE_URL`, `XAI_MODEL` (default `grok-4`),
  `XAI_TIMEOUT_MS`, `XAI_INPUT_MICROS_PER_TOKEN`,
  `XAI_OUTPUT_MICROS_PER_TOKEN`; the key never appears in
  `describeConfig`; the API boot shares one client between both
  adapters and logs that no live call has been verified.
- Docs: `agent-permissions.md` (provider row, data sent), OD-19
  (decided, with what stays open), `provider-capabilities.md` (both
  model rows), `source-register.md` (SR-XAI-01, blocked), `operations.md`,
  `.env.example`, README.
- Devnet deployment preparation: a deployer wallet
  (`GnryRwBNqEUWiEWesrTW3EQMf2MKahCZVrd5fefoZCvZ`) and a program keypair
  (`983sncE9Q3yfS1XXeCyDsdvpkA3ks4fNurMHALCZgRPp`) were generated outside
  the repository (session scratchpad, devnet only; secrets not committed).
  The deployment itself is blocked: the environment's network policy
  denies `api.devnet.solana.com`, `release.anza.xyz` and GitHub releases,
  and no Solana CLI or platform tools are installed (Rust and cargo are).

## Commands and results

| Command | Result |
| ------- | ------ |
| `pnpm exec vitest run --project node packages/model-xai packages/config/test` | 7 adapter tests and 29 config tests passed |
| `pnpm build`, `pnpm exec tsc -p tsconfig.tests.json --noEmit`, `pnpm boundaries:check` | clean; 35 workspace packages within their boundaries |
| `pnpm verify` (PostgreSQL and the Temporal dev server running) | green on its first run: lint at the baseline, typecheck, 93 test files with 692 tests, boundaries, OpenAPI and client drift checks, migrations, tokens, the web build and the docs build |
| `curl` probes of `api.x.ai`, `api.devnet.solana.com`, `release.anza.xyz`, `github.com/anza-xyz/agave/releases` | every host refused by the proxy (CONNECT 403); nothing retrieved, nothing deployed |

## Provider verification status

`research.model.generate` and `companion.model.run` stay BLOCKED: the
xAI adapter is IMPLEMENTED and verified against an in-process stand-in;
no live call was possible from the build environment (SR-XAI-01), so the
model list, the price list and the terms are unverified. Enabling `xai`
outside local requires `XAI_API_KEY`, the terms and retention recorded in
`docs/markov/agent-permissions.md` and one live run recorded in
`docs/markov/provider-capabilities.md`. `registry.strategy.publish` stays
FIXTURE_VERIFIED: nothing was deployed.

## Unresolved risks and dependencies

- The chat completions shape and the default prices come from carried
  knowledge, not from a retrieved page; the first live call must confirm
  both before the cost budgets mean anything.
- `XAI_MODEL` defaults to `grok-4`; production should pin the exact model
  the terms were reviewed for.
- The devnet deployment needs network access to the hosts above (or a
  machine with the Solana CLI), a funded deployer and the `declare_id`
  change with regenerated vectors, as `docs/markov/strategy-registry.md`
  describes.

## Next

Meteora read-only pool observation and DBC simulation as research tools
with capability states (launches disabled), the provider readiness matrix,
then B18.
