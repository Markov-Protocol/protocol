{
  "name": "@markov/registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@markov/contracts": "workspace:*",
    "@markov/solana-rpc": "workspace:*"
  },
  "devDependencies": {},
  "description": "Strategy registry SDK: program-derived addresses, the exact instruction and account encodings of programs/strategy-registry, the rule mirror, legacy transaction encoding and verification, and the in-memory fixture ledger for tests"
}
