{
  "name": "@markov/accounting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@markov/contracts": "workspace:*",
    "@markov/planning": "workspace:*",
    "@markov/solana-codec": "workspace:*"
  },
  "devDependencies": {},
  "description": "Accounting rules (B12): the balanced append-only quantity journal in raw units with one equation per asset, FIFO lot attribution as analytics bookkeeping, reconciliation of the journal against chain balances with explicit external flows, and canonical signed receipts with verification; no HTTP, database or provider SDK"
}
